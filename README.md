# Gym Management System

Responsive gym management dashboard with Firebase Authentication and Firestore.

## Upload to GitHub
Upload all files to the repository root, including the `assets` folder.

## Firebase
Enable Email/Password and Google sign-in. Create Firestore Database and publish rules that allow each signed-in user to read/write only their own `users/{uid}` data.
